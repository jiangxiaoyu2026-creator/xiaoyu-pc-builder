version: '3.8'

services:
  app:
    build: .
    container_name: xiaoyu-pc-builder
    ports:
      - "8000:8000"
    env_file:
      - .env
    restart: always
    environment:
      - PORT=8000
    volumes:
      - ./data:/app/data
  # SQLite doesn't need a separate service as it's a file in the persistence volume
  # The server container should mount a volume to persist the .db file
