version: '3.8'

services:
  app:
    build: .
    container_name: xiaoyu-pc-builder
    ports:
      - "3001:3001"
    env_file:
      - .env
    restart: always
    environment:
      - NODE_ENV=production
      - PORT=3001
